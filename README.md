# mtchack

## Запуск

1. Перейдите в папку `wikilive`.
2. Скопируйте `.env.example` в `.env`.
3. Заполните значения в `.env`.
4. Запустите проект:

```bash
cd wikilive
docker compose up --build
```

5. Откройте `http://localhost:3000`.
