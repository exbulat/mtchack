services:
  postgres:
    container_name: postgres
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - '5433:5432'
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-wikilive}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-wikilive_secure_min_12_chars}
      POSTGRES_DB: ${POSTGRES_DB:-wikilive}
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U wikilive']
      interval: 5s
      timeout: 3s
      retries: 5

  backend:
    container_name: backend
    build: ./backend
    restart: unless-stopped
    ports:
      - '3001:3001'
      - '3002:3002'
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-wikilive}:${POSTGRES_PASSWORD:-wikilive_secure_min_12_chars}@postgres:5432/wikilive
      MWS_TABLES_BASE_URL: ${MWS_TABLES_BASE_URL:-https://tables.mws.ru}
      MWS_TABLES_TOKEN: ${MWS_TABLES_TOKEN:-}
      MWS_TABLES_SPACE_ID: ${MWS_TABLES_SPACE_ID:-}
      MWS_GPT_API_URL: ${MWS_GPT_API_URL:-https://api.gpt.mws.ru/v1/chat/completions}
      MWS_GPT_API_KEY: ${MWS_GPT_API_KEY:-}
      PORT: 3001
      WS_PORT: 3002
      ALLOWED_ORIGIN: ${ALLOWED_ORIGIN:-http://localhost:3000}
      COOKIE_SECRET: ${COOKIE_SECRET:-wikilive-dev-cookie-secret-change-me}
    depends_on:
      postgres:
        condition: service_healthy

  frontend:
    container_name: frontend
    build: ./frontend
    restart: unless-stopped
    ports:
      - '3000:8080'
    depends_on:
      - backend

volumes:
  pgdata:
